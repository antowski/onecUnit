
Процедура Конструктор()
	
КонецПроцедуры // Конструктор

Процедура тестОшибкаЕслиКлассаТестаНеСуществует() Экспорт
	
	Фабрика = СоздатьОбъект("ФабрикаТестов");
	
	ИмяНесуществующегоКласса = "ИмяНесуществующегоКласса";
	ОжидаемаяОшибка = "Не обнаружен класс '" + ИмяНесуществующегоКласса + "'";
	
	ИсключениеВыброшено = 0;
	
	Попытка
		Фабрика.СоздатьТест(ИмяНесуществующегоКласса, "НесуществующийМетод");
	Исключение
		ИсключениеВыброшено = 1;
		я().Проверить.НеРавно(0, Найти(ОписаниеОшибки(), ОжидаемаяОшибка));
	КонецПопытки;
	
	я().Проверить.Равно(1, ИсключениеВыброшено);
	
	//я()._ВыброситьИскл("Ожидалось исключение, а выброшено оно не было" );
	
КонецПроцедуры // тестОшибкаЕслиКлассаТестаНеСуществует
