
// vim: syntax=1C Enterprise:

Перем Проверить Экспорт;
Перем мИмяТестовогоМетода;

Процедура Конструктор()
	Проверить = СоздатьОбъект("Утверждения");
КонецПроцедуры

Функция Представление() Экспорт
	Возврат мИмяТестовогоМетода;
КонецФункции

Процедура Провален(пТекстОшибки) Экспорт
	Утверждения = СоздатьОбъект("Утверждения");
	Утверждения.Провален(пТекстОшибки);
КонецПроцедуры // Провален

Процедура ПриЗаписи_Проверить()
	Возврат;
КонецПроцедуры // ПриЗаписи_Проверить

Процедура Инит(пИмяТестовогоМетода) Экспорт
	мИмяТестовогоМетода = пИмяТестовогоМетода;
КонецПроцедуры // Инит

Процедура ПередЗапуском() Экспорт
	
КонецПроцедуры // ПередЗапуском

Процедура Выполнить(пРезультат) Экспорт
	
	пРезультат.ЗарегистрироватьЗапуск(я());
	
	Попытка
		вирт().ПередЗапуском();		
	Исключение
		пРезультат.ЗарегистрироватьПровал(я(), ОписаниеОшибки());
		Возврат;
	КонецПопытки;	
	
	Делегат = СоздатьОбъект("Делегат");
	Делегат.ОпределитьТип(0,0);
	
	Попытка
		Делегат.Добавить(вирт(), мИмяТестовогоМетода);
		Делегат.Вызвать();
	Исключение
		пРезультат.ЗарегистрироватьПровал(я(), ОписаниеОшибки());
	КонецПопытки;	
	
	пРезультат.ЗарегистрироватьЗавершение(я());
	
	вирт().ПослеЗапуска();
	
КонецПроцедуры

Процедура ПослеЗапуска() Экспорт
	
КонецПроцедуры // ПослеЗапуска
