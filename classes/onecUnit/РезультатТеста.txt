
Перем мКолЗапущено;
Перем мКолПровалено;
Перем мПодписчики;

Процедура Конструктор()
	мКолЗапущено = 0;
	мКолПровалено = 0;
	мПодписчики = СоздатьОбъект("СписокЗначений");
КонецПроцедуры // Конструктор

Процедура ЗарегистрироватьЗапуск(пТест) Экспорт
	
	мКолЗапущено = мКолЗапущено + 1;
	
	Для й = 1 По мПодписчики.РазмерСписка() Цикл
		Подписчик = мПодписчики.ПолучитьЗначение(й);
		Подписчик.ПриЗапуске(пТест);
	КонецЦикла;
	
КонецПроцедуры // ЗарегистрироватьЗапуск

Процедура ЗарегистрироватьПровал(пТест, пОшибкаСтр) Экспорт
	
	мКолПровалено = мКолПровалено + 1;
	
	Для й = 1 По мПодписчики.РазмерСписка() Цикл
		Подписчик = мПодписчики.ПолучитьЗначение(й);
		Подписчик.ПриПровале(пТест, пОшибкаСтр);
	КонецЦикла;
	
КонецПроцедуры // ЗарегистрироватьПровал

Процедура ЗарегистрироватьЗавершение(пТест) Экспорт
	
	Для й = 1 По мПодписчики.РазмерСписка() Цикл
		Подписчик = мПодписчики.ПолучитьЗначение(й);
		Подписчик.ПриЗавершении(пТест);
	КонецЦикла;
	
КонецПроцедуры // ЗарегистрироватьЗавершение

Процедура ДобавитьПодписчика(пОбъектПодписчик) Экспорт
	мПодписчики.ДобавитьЗначение(пОбъектПодписчик);
КонецПроцедуры // ДобавитьПодписчика

Функция ПолучитьКолЗапущено() Экспорт
	Возврат мКолЗапущено;
КонецФункции // ПолучитьКолЗапущено

Функция ПолучитьКолПровалено() Экспорт
	Возврат мКолПровалено;
КонецФункции // ПолучитьКолПровалено
